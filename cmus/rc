# $HOME/.cmus/rc personal configuration file

cd ~/music/playlists

unbind -f common z
bind -f common w player-prev

unbind -f common ,
unbind -f common .
bind -f common < seek -1m
bind -f common > seek +1m
